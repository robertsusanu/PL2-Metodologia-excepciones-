/*
public class ScannerFromFileWithFinally {
     public static void main(String[] args) {
        try {
// Main logic here
            //open file;
            //process file;
            //Esto es lo primero que hace el programa, y si ocurre una excepcion pasa al catch
        } catch (FileNotFoundException ex) { // Exception handlers below
// Exception handler for "file not found"
            //Comprueba si no encuentra el archivo
        } catch (IOException ex) {
// Exception handler for "IO errors"
        } finally {
            //close file; // always try to close the file
            // Despues de hacer la logica principal y tratar las excepciones, accede aqui y cierra el archivo
        }
    }
}

     */
